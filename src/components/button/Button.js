import React, {Component} from 'react';
import './button.css';

class Button extends Component {

   componentDidMount() {

   };

   render() {
      return (
         <button>Click</button>
      );
   }
}

export default Button;