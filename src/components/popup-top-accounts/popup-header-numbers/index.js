import { connect } from "react-redux";
import PopupHeaderNumbers from './PopupHeaderNumbers';

const mapStateToProps = (state) => {
   return {
   }
};

const mapDispatchToProps = (dispatch) => {
   return {
   }
};

export default connect(mapStateToProps, mapDispatchToProps)(PopupHeaderNumbers)